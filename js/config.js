/* ============================================================
   config.js – Configuration
   ============================================================ */
window.Trikono = window.Trikono || {};

window.Trikono.Config = {
    // ── Metered.ca TURN servers (free tier: 50 GB/month) ──────
    // 1. Sign up at https://www.metered.ca/signup
    // 2. Create an App in the dashboard
    // 3. Copy the API key and paste it below
    METERED_API_KEY: '',
};
